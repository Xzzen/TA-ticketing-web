// 1. KONFIGURASI
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// 2. MODEL USER
model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  password  String
  
  // REVISI: Ganti Enum jadi String agar bisa jalan di SQLite
  role      String   @default("USER") 
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  transactions Transaction[]
  events       Event[]
}

// 3. MODEL EVENT
model Event {
  id          Int      @id @default(autoincrement())
  name        String
  description String
  date        DateTime
  location    String
  image       String?
  organizerId Int
  organizer   User     @relation(fields: [organizerId], references: [id], onDelete: Cascade)
  
  // --- TAMBAHAN BARU (SOFT DELETE) ---
  // Jika kosong (null) = Aktif. Jika ada tanggalnya = Terhapus.
  deletedAt   DateTime?

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  tickets     Ticket[]
}

// 4. MODEL TICKET
model Ticket {
  id        Int      @id @default(autoincrement())
  name      String   
  price     Float
  quota     Int
  eventId   Int
  event     Event    @relation(fields: [eventId], references: [id], onDelete: Cascade)
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  transactionDetails TransactionDetail[]
}

// 5. MODEL TRANSACTION
model Transaction {
  id          Int      @id @default(autoincrement())
  userId      Int
  user        User     @relation(fields: [userId], references: [id])
  totalAmount Float
  
  // REVISI: Ganti Enum jadi String agar bisa jalan di SQLite
  status      String   @default("PENDING")
  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  details     TransactionDetail[]
}

// 6. JUNCTION TABLE
model TransactionDetail {
  id            Int         @id @default(autoincrement())
  transactionId Int
  transaction   Transaction @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  ticketId      Int
  ticket        Ticket      @relation(fields: [ticketId], references: [id])
  
  quantity      Int
  subtotal      Float
}

// BAGIAN ENUM DIHAPUS KARENA SQLITE TIDAK SUPPORT